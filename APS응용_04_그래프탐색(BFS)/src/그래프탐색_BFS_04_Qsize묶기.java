/**
 * 큐의 현재 크기만큼을 한 묶음(레벨)으로 처리해 미로의 최단 길이를 구한다.
 *
 * <p>BFS 큐에는 같은 거리의 칸들이 연속해서 들어온다. while 반복이 시작될 때의
 * queue.size()를 미리 저장하고 그 개수만큼만 꺼내면 한 거리 레벨이 끝난다.
 * 그 뒤 distance를 1 증가시키면 거리 배열이나 거리 멤버 없이 길이를 셀 수 있다.</p>
 *
 * <p>for문의 조건에서 queue.size()를 계속 읽으면 처리 도중 새로 들어온 다음 레벨까지
 * 같은 레벨로 처리할 수 있으므로, 반드시 반복 전에 size 변수로 고정해야 한다.</p>
 */
public class 그래프탐색_BFS_04_Qsize묶기 {

    public static void main(String[] args) {
    }

}
